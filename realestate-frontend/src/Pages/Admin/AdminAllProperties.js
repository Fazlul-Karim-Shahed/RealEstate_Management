import React from 'react'
import { connect } from 'react-redux'


const mapStateToProps = (state) => ({})

export const AdminProperties = (props) => {
  return (
    <div>Admin all Properties</div>
  )
}




export default connect(mapStateToProps)(AdminProperties)